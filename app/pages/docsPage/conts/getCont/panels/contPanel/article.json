[
  {
    "type":"section",
    "title":"Get Cont Module Api",
    "items":[
      {
        "type":"para",
        "string":"this api takes page and cont name as strings for argument and fetches the cont module if already is loaded in the app."
      },
      {
        "type":"code",
        "string":"const contModule = engine.get.contModule('homePage','loginCont')"
      }
    ]
  }
]
